// PublicNavbar delegates to the shared landing Header component.
// Both the landing page and the job board now use the same polished header.
export { default as PublicNavbar } from "@/features/landing/components/Header";